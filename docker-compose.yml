version: "3"

services:
  backend:
    image: nokKnock/backend
    container_name: backend
    hostname: backend
    env_file:
      - .env
    expose:
      - "8080"

  nginx:
    image: nokKnock/nginx
    depends_on:
      - backend
    restart: always
    ports:
      - "80:80"